# 🦅 АҚШ Үкіметі — Американдық Үкімет туралы сайт

## Стек
- **React 18** + **TypeScript**
- **Vite** (сборка)
- **Tailwind CSS v3** (барлық стильдер Tailwind класстары арқылы)

## Іске қосу

```bash
# 1. Тәуелділіктерді орнату
npm install

# 2. Dev сервер іске қосу
npm run dev

# 3. Браузерде ашу
# http://localhost:5173
```

## Продакшн үшін жинау

```bash
npm run build
# Дайын файлдар /dist папкасында болады
```

## Жоба құрылымы

```
us-government/
├── index.html
├── package.json
├── vite.config.ts
├── tailwind.config.js        ← Tailwind баптаулары
├── postcss.config.js
├── tsconfig.json
└── src/
    ├── main.tsx              ← Кіру нүктесі
    ├── App.tsx               ← Басты компонент
    ├── index.css             ← @tailwind директивалары
    ├── data.ts               ← Барлық мазмұн деректері
    ├── hooks/
    │   └── useInView.ts      ← Scroll анимация хуку
    ├── components/
    │   ├── Counter.tsx       ← Анимациялық санағыш
    │   ├── Fade.tsx          ← Scroll-да пайда болу
    │   ├── SectionHeader.tsx ← Бөлім тақырыбы
    │   └── PhotoSlot.tsx     ← Фото жүктеу слоты
    └── sections/
        ├── Navbar.tsx        ← Навигация
        ├── Hero.tsx          ← Бас экран
        ├── Stats.tsx         ← Статистика
        ├── Branches.tsx      ← Билік тармақтары
        ├── Congress.tsx      ← Конгресс
        ├── Powers.tsx        ← Өкілеттіктер
        ├── Gallery.tsx       ← Фотогалерея
        └── History.tsx       ← Тарих
```

## Мазмұнды өзгерту
Барлық мәтін мен деректер `src/data.ts` файлында — оңай өңдеуге болады.
